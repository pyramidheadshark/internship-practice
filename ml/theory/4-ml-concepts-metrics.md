---
title: Основные концепции машинного обучения и метрики
description:  Типы машинного обучения, процесс разработки ML-модели, метрики оценки моделей (регрессии и классификации), переобучение и недообучение, bias-variance tradeoff. Расширенное объяснение bias и variance.
published: true
date: 2025-02-25T19:15:00.000Z
tags: ml, selfstudy, основы ML, метрики, переобучение, недообучение, bias, variance
editor: markdown
dateCreated: 2025-02-25T19:15:00.000Z
---

# 🧠 Концепции ML и метрики: Ключ к оценке качества моделей

Раздел посвящен **фундаментальным концепциям машинного обучения и инструментам оценки качества моделей.**  В данном разделе углубимся в понимание ключевых понятий, которые лежат в основе ML-разработки и позволяют строить, оценивать и улучшать модели.

## 1. 🤖 Типы машинного обучения: Обзор задач

Машинное обучение (ML) решает широкий спектр задач, которые традиционно делятся на три основных типа:

*   **Обучение с учителем (Supervised Learning) 👨‍🏫:**
    *   **Суть:**  Модель обучается на размеченных данных, где для каждого объекта известна "правильный ответ" (целевая переменная).  Цель - научиться **предсказывать** этот "правильный ответ" для новых, неразмеченных объектов.
    *   **Типичные задачи:**
        *   **Классификация (Classification):**  Предсказание **категориальной** целевой переменной (например, определение, является ли письмо спамом или нет, классификация изображений на кошек и собак).
        *   **Регрессия (Regression):**  Предсказание **числовой** целевой переменной (например, предсказание цены дома, температуры, спроса на товар).
    *   **Примеры алгоритмов:**  Линейная регрессия, Логистическая регрессия, Деревья решений, Случайный лес, Метод опорных векторов (SVM), Нейронные сети.

*   **Обучение без учителя (Unsupervised Learning) 🧑‍🏫:**
    *   **Суть:**  Модель обучается на **неразмеченных данных**, пытаясь найти в них **скрытые закономерности, структуры и зависимости**.  Цель - **понять структуру данных**, а не предсказывать конкретные значения.
    *   **Типичные задачи:**
        *   **Кластеризация (Clustering):**  Разбиение данных на **группы (кластеры)** схожих объектов (например, сегментация клиентов, выделение групп похожих документов).
        *   **Понижение размерности (Dimensionality Reduction):**  Уменьшение количества признаков, **сохраняя при этом важную информацию** (например, сжатие изображений, визуализация многомерных данных).
        *   **Поиск аномалий (Anomaly Detection):**  Выявление **необычных, "выбивающихся" объектов** (например, обнаружение мошеннических транзакций, выявление дефектов производства).
    *   **Примеры алгоритмов:**  K-средних (K-Means), DBSCAN, PCA, t-SNE, Автоэнкодеры.

*   **Обучение с подкреплением (Reinforcement Learning) 🎮:**
    *   **Суть:**  Модель (**агент**) обучается **действовать в среде**, чтобы **максимизировать награду**.  Агент учится путем **проб и ошибок**, получая **подкрепление** (награду или штраф) за свои действия.
    *   **Типичные задачи:**  Управление роботами, игры, персонализация рекомендаций, оптимизация стратегий.
    *   **Ключевые понятия:**  Агент, среда, действие, состояние, награда, стратегия (policy).
    *   **Примеры алгоритмов:**  Q-Learning, SARSA, Deep Q-Networks (DQN), Policy Gradient methods.

## 2. ⚙️ Процесс разработки ML-модели: Этапы на пути к успеху

Разработка ML-модели – это **итеративный процесс**, включающий несколько ключевых этапов:

1.  **Постановка задачи и сбор данных 🎯:**
    *   **Определение бизнес-цели:**  Чего мы хотим достичь с помощью ML? (например, увеличить продажи, снизить отток клиентов, автоматизировать процесс).
    *   **Формулировка ML-задачи:**  Какой тип задачи ML (классификация, регрессия, кластеризация и т.д.) соответствует бизнес-цели?
    *   **Сбор данных:**  Поиск и сбор данных, необходимых для решения задачи.  **Данные – "топливо" для ML.**  Важно обеспечить **качество и репрезентативность данных**.

2.  **Исследовательский анализ данных (EDA) 🔍:**
    *   **Понимание данных:**  Изучение **структуры, типов данных, распределений, взаимосвязей, пропущенных значений, аномалий**.
    *   **Визуализация данных:**  Использование графиков и диаграмм для **наглядного представления данных** и выявления закономерностей (гистограммы, точечные диаграммы, коробчатые диаграммы, тепловые карты и т.д.).
    *   **Формирование гипотез:**  Выдвижение предположений о том, какие признаки могут быть важны для модели, какие закономерности могут существовать.  **EDA помогает лучше понять задачу и данные.**

3.  **Предобработка и подготовка данных 🧹:**
    *   **Очистка данных:**  Обработка **пропущенных значений (imputation)**, **выбросов (outlier removal/handling)**, **дубликатов**.
    *   **Трансформация данных:**  **Масштабирование признаков (scaling/normalization)**, **кодирование категориальных признаков (one-hot encoding, label encoding)**, **генерация новых признаков (feature engineering)**.
    *   **Разделение данных:**  Разделение на **обучающую (train), валидационную (validation) и тестовую (test) выборки**.  **Валидация и тест необходимы для оценки обобщающей способности модели.**

4.  **Выбор модели и обучение 🏋️:**
    *   **Выбор алгоритма:**  Подбор **подходящего алгоритма ML** в зависимости от типа задачи, размера данных, требований к интерпретируемости и производительности.  Часто **начинают с простых моделей** (линейные модели, деревья решений) и **постепенно переходят к более сложным** (ансамбли, нейросети).
    *   **Обучение модели (training):**  **Настройка параметров модели** на обучающей выборке с использованием **функции потерь (loss function)** и **методов оптимизации (optimization algorithms)** (например, градиентный спуск).

5.  **Оценка модели и выбор метрик 🎯:**
    *   **Выбор метрик:**  Определение **ключевых метрик** для оценки качества модели в зависимости от типа задачи и бизнес-целей (Accuracy, Precision, Recall, F1-score, ROC AUC для классификации; MSE, MAE, RMSE, R-squared для регрессии).
    *   **Оценка на валидационной выборке:**  Измерение качества обученной модели на **валидационной выборке** для **настройки гиперпараметров** и **выбора лучшей модели**.  **Валидация имитирует "новые" данные.**
    *   **Кросс-валидация (Cross-validation):**  Более надежная оценка качества модели, чем однократное разбиение на train/validation.  Особенно важна при **ограниченном размере данных**.

6.  **Настройка гиперпараметров и улучшение модели ⚙️:**
    *   **Подбор гиперпараметров:**  **Оптимизация гиперпараметров модели** (параметров, которые не обучаются, а задаются "извне") с использованием **валидационной выборки** или **кросс-валидации** (например, GridSearchCV, RandomizedSearchCV).
    *   **Feature Engineering:**  **Улучшение признаков**, добавление новых, удаление неинформативных.
    *   **Выбор другой модели:**  Переход к **более сложной или другой модели**, если текущая не обеспечивает достаточного качества.
    *   **Итерации:**  **Возврат к предыдущим этапам** (сбор данных, EDA, предобработка) для улучшения модели.  **ML-разработка – это цикл.**

7.  **Тестирование модели 🧪:**
    *   **Финальная оценка на тестовой выборке:**  Измерение качества **лучшей модели** (выбранной на этапе валидации) на **тестовой выборке**.  **Тест – "последний рубеж" перед "боевым" применением.**  Тестовая выборка должна быть **полностью "невидимой"** на этапах обучения и валидации.
    *   **Оценка обобщающей способности:**  Проверка, насколько хорошо модель **обобщает** знания на **новые, реальные данные**.

8.  **Развертывание и мониторинг 🚀:**
    *   **Внедрение модели в production:**  Интеграция модели в **реальные бизнес-процессы** или продукты.
    *   **Мониторинг производительности:**  **Отслеживание качества модели** в "боевых" условиях, **сбор online-метрик**, **реагирование на ухудшение качества** (retraining, доработка модели).
    *   **Обслуживание и обновление модели:**  **Регулярное обновление модели** с новыми данными, **адаптация к изменениям** в данных или бизнес-задачах.

## 3. 📏 Оценка моделей и метрики: Измеряем качество и успех

Подробно изучены **метрики для оценки качества моделей**, разделенные на метрики **регрессии** и **классификации**:

### 3.1. Метрики регрессии 📏:

*   **MSE (Mean Squared Error) - Среднеквадратичная ошибка:**  Среднее значение **квадратов ошибок** (различий между предсказанными и истинными значениями).  **Чем меньше MSE, тем лучше.**  Сильно **чувствительна к выбросам** из-за квадратирования.
    \[
    MSE = \frac{1}{n} \sum_{i=1}^{n} (y_i - \hat{y}_i)^2
    \]
    где \(y_i\) - истинное значение, \(\hat{y}_i\) - предсказанное значение, \(n\) - количество объектов.

*   **MAE (Mean Absolute Error) - Средняя абсолютная ошибка:**  Среднее значение **абсолютных ошибок**.  **Менее чувствительна к выбросам**, чем MSE.  Легче интерпретировать, чем MSE - это просто **средняя ошибка в исходных единицах измерения**.
    \[
    MAE = \frac{1}{n} \sum_{i=1}^{n} |y_i - \hat{y}_i|
    \]

*   **MAPE (Mean Absolute Percentage Error) - Средняя абсолютная процентная ошибка:**  Среднее значение **процентных ошибок**.  **Удобна для интерпретации в процентах**.  **Нельзя использовать, если в данных есть нулевые значения** целевой переменной.  **Несимметрична** - штрафует за **занижение прогноза сильнее, чем за завышение**.
    \[
    MAPE = \frac{1}{n} \sum_{i=1}^{n} \left| \frac{y_i - \hat{y}_i}{y_i} \right| \times 100\%
    \]

*   **RMSE (Root Mean Squared Error) - Корень из среднеквадратичной ошибки:**  Квадратный корень из MSE.  **Интерпретируется в тех же единицах, что и целевая переменная.**  Также **чувствительна к выбросам**, как и MSE.  Часто используется, так как **более интерпретируема, чем MSE** (легче сравнивать с масштабом целевой переменной).
    \[
    RMSE = \sqrt{MSE} = \sqrt{\frac{1}{n} \sum_{i=1}^{n} (y_i - \hat{y}_i)^2}
    \]

*   **R-squared (Коэффициент детерминации)** -  показывает, какая доля **дисперсии** целевой переменной **объясняется моделью**.  Значение от 0 до 1 (иногда может быть отрицательным).  **Чем ближе к 1, тем лучше модель.**  Полезен для **сравнения разных моделей** на одних и тех же данных.  **Недостаток:**  **не штрафует за добавление новых признаков**, даже если они не улучшают модель (может расти при добавлении "шумовых" признаков).
    \[
    R^2 = 1 - \frac{SS_{res}}{SS_{tot}} = 1 - \frac{\sum_{i=1}^{n} (y_i - \hat{y}_i)^2}{\sum_{i=1}^{n} (y_i - \bar{y})^2}
    \]
    где \(SS_{res}\) - сумма квадратов остатков (residual sum of squares), \(SS_{tot}\) - общая сумма квадратов (total sum of squares), \(\bar{y}\) - среднее значение целевой переменной.

*   **Adjusted R-squared (Скорректированный R-squared)** -  модификация R-squared, которая **штрафует за добавление новых признаков**, не улучшающих модель.  Используется для **сравнения моделей с разным количеством признаков**, чтобы избежать переобучения за счет добавления "шумовых" признаков.  **Всегда меньше или равен R-squared**.
    \[
    Adjusted\ R^2 = 1 - \frac{(1-R^2)(n-1)}{n-p-1}
    \]
    где \(p\) - количество признаков в модели.

*   **Когда выбирать RMSE vs MAE:**  **RMSE** более чувствительна к **выбросам** из-за квадратирования ошибок.  Если **выбросы важны** и вы хотите их "штрафовать" сильнее, выбирайте RMSE.  Если **выбросы не важны** или вы хотите **более робастную к выбросам метрику**, выбирайте MAE.  **MAE легче интерпретировать** - это просто средняя абсолютная ошибка в исходных единицах.

### 3.2. Метрики классификации 📊:

*   **Accuracy (Точность):**  Доля **правильных ответов** модели от общего количества объектов.  **Проста и понятна, но малоинформативна при несбалансированных классах** (когда один класс встречается гораздо чаще другого).
    \[
    Accuracy = \frac{Number\ of\ correct\ predictions}{Total\ number\ of\ predictions} = \frac{TP + TN}{TP + TN + FP + FN}
    \]
    где TP - True Positives (верно положительные), TN - True Negatives (верно отрицательные), FP - False Positives (ложно положительные), FN - False Negatives (ложно отрицательные).

*   **Confusion Matrix (Матрица ошибок):**  **Таблица сопряженности**, показывающая **распределение предсказаний модели по классам** в сравнении с истинными классами.  Позволяет увидеть, **какие типы ошибок** делает модель (кого чаще путает с кем).  **Основа для расчета Precision, Recall, F1-score.**

    |                  | Predicted Positive | Predicted Negative |
    | :--------------- | :----------------- | :----------------- |
    | **Actual Positive** | TP                 | FN                 |
    | **Actual Negative** | FP                 | TN                 |

*   **Precision (Точность, PPV - Positive Predictive Value):**  Доля **верно предсказанных положительных объектов** от **всех объектов, предсказанных как положительные**.  **Важна, когда нужно минимизировать False Positives** (ошибки первого рода, "ложные тревоги").  Например, в задаче определения спама - Precision показывает, какая доля писем, **отмеченных как спам**, действительно является спамом.
    \[
    Precision = \frac{TP}{TP + FP}
    \]

*   **Recall (Полнота, TPR - True Positive Rate, Sensitivity - Чувствительность):**  Доля **верно предсказанных положительных объектов** от **всех истинно положительных объектов**.  **Важна, когда нужно минимизировать False Negatives** (ошибки второго рода, "пропуски").  Например, в задаче медицинской диагностики - Recall показывает, какая доля **действительно больных** людей была **верно идентифицирована** как больные.
    \[
    Recall = \frac{TP}{TP + FN}
    \]

*   **F1-score (F-мера, Гармоническое среднее Precision и Recall):**  **Баланс между Precision и Recall**.  Используется, когда **важно учитывать обе метрики одновременно**.  **Полезна при несбалансированных классах**.  **Гармоническое среднее** штрафует за **дисбаланс между Precision и Recall** сильнее, чем арифметическое среднее.
    \[
    F_1 = 2 \times \frac{Precision \times Recall}{Precision + Recall}
    \]

*   **ROC AUC (Area Under the ROC Curve) - Площадь под ROC-кривой:**  **ROC-кривая (Receiver Operating Characteristic Curve)** - график зависимости **True Positive Rate (Recall)** от **False Positive Rate (FPR)** при **различных порогах классификации**.  **AUC** - площадь под этой кривой.  **AUC-ROC показывает, насколько хорошо модель различает классы в целом, не завися от порога классификации.**  **AUC = 0.5** - случайное угадывание, **AUC = 1** - идеальная классификация.  **Менее чувствительна к несбалансированности классов, чем Accuracy.**

    \[
    FPR = \frac{FP}{FP + TN} = \frac{False\ Positives}{Actual\ Negatives}
    \]

*   **PR AUC (Area Under the Precision-Recall Curve) - Площадь под PR-кривой:**  **PR-кривая (Precision-Recall Curve)** - график зависимости **Precision** от **Recall** при **различных порогах классификации**.  **AUC** - площадь под этой кривой.  **AUC-PR показывает, насколько хорошо модель справляется с задачей классификации положительного класса, особенно в условиях несбалансированных классов.**  **Более информативна, чем ROC AUC при сильном дисбалансе классов**, когда положительный класс встречается редко.  **Сосредоточена на качестве предсказаний именно положительного класса.**

*   **Log Loss (Logarithmic Loss, Cross-Entropy Loss) - Логарифмическая потеря, Кросс-энтропия:**  Метрика, используемая **в задачах классификации, где модель предсказывает вероятности классов**.  **Измеряет "уверенность" модели в неправильных ответах**.  **Чем меньше Log Loss, тем лучше.**  **Штрафует за высокую уверенность в неправильном классе сильнее, чем за неуверенность.**  **Используется и как функция потерь при обучении логистической регрессии и нейросетей.**
    \[
    Log Loss = - \frac{1}{n} \sum_{i=1}^{n} [y_i \log(p_i) + (1 - y_i) \log(1 - p_i)]
    \]
    где \(p_i\) - предсказанная вероятность положительного класса для i-го объекта, \(y_i\) - истинный класс (0 или 1).  Для **мультиклассовой классификации** формула обобщается.

*   **Мультиклассовые метрики:**  Для задач классификации с **более чем двумя классами** (мультиклассовая классификация) Accuracy, Precision, Recall, F1-score обобщаются.  Существуют разные способы усреднения Precision, Recall, F1-score по классам:
    *   **Macro-average:**  **Среднее арифметическое метрик по всем классам**.  **Все классы равноправны**, не учитывается частота классов.  **Подходит, когда важны результаты классификации для всех классов одинаково, независимо от их размера.**
    *   **Micro-average:**  **Глобальные TP, FP, FN суммируются по всем классам, и на их основе рассчитываются Precision, Recall, F1-score**.  **Учитывает частоту классов**.  **Подходит, когда важна общая производительность классификатора, и классы могут быть несбалансированы.**  В micro-average Accuracy, Precision, Recall, F1-score обычно равны.
    *   **Weighted-average:**  **Среднее взвешенное метрик по классам, где веса – количество объектов в каждом классе**.  **Учитывает размер классов**.  **Похож на micro-average, но Precision, Recall, F1-score могут различаться.**  **Подходит, когда важна производительность на каждом классе, но с учетом их распространенности.**

*   **Когда выбирать AUC-PR vs AUC-ROC:**  **ROC AUC** - **хороша в общем случае**, показывает качество классификации **для всех классов в целом**.  **PR AUC** - **предпочтительнее при сильном дисбалансе классов**, особенно когда **положительный класс (который мы хотим детектировать) встречается редко**.  PR AUC **фокусируется на качестве предсказаний именно редкого положительного класса**, что часто важнее в задачах, где "пропуск" положительного класса критичен (медицинская диагностика, обнаружение мошенничества).

### 3.3. 🔬 Статистическая значимость метрик и валидация моделей

*   **Статистическая значимость метрик:**  Оценка, **насколько можно доверять полученным значениям метрик**.  **Случайные колебания** в данных могут влиять на метрики.  Важно понимать, является ли **различие в метриках между двумя моделями случайным или статистически значимым**.
    *   **Доверительные интервалы (Confidence Intervals):**  Диапазон значений, в котором, **с определенной вероятностью**, находится **истинное значение метрики**.  **Узкий доверительный интервал – более точная оценка метрики.**  Строятся, например, с помощью **бутстрапа (bootstrap)**.
    *   **p-value (p-значение):**  Вероятность получить **наблюдаемое различие в метриках (или большее)** между двумя моделями, **если на самом деле разницы нет (нулевая гипотеза)**.  **Маленький p-value (обычно < 0.05) – отвергаем нулевую гипотезу, различие статистически значимо.**  Используется, например, в **A/B-тестировании моделей**.
    *   **Power analysis (Анализ мощности):**  Оценка **вероятности обнаружить статистически значимое различие**, если оно **действительно есть**.  **Важна при планировании экспериментов**, чтобы определить **необходимый размер выборки** для выявления эффекта.

*   **Валидация моделей: Кросс-валидация (Cross-validation):**  Метод **более надежной оценки обобщающей способности модели**, чем однократное разбиение на train/validation.  **Особенно важна при ограниченном размере данных.**
    *   **k-fold Cross-validation (k-блочная кросс-валидация):**  Данные разбиваются на **k блоков (fold)**.  **k раз модель обучается на \(k-1\) блоках и оценивается на оставшемся блоке**.  Метрики **усредняются по k итерациям**.  **Часто используется k=5 или k=10.**
    *   **Stratified k-fold Cross-validation (Стратифицированная k-блочная кросс-валидация):**  **Сохраняет пропорции классов** в каждом блоке.  **Важна для задач классификации с несбалансированными классами.**
    *   **Leave-one-out Cross-validation (LOOCV) (Кросс-валидация "исключение по одному"):**  **Каждый объект используется как валидационный блок по одному разу**.  **k равно количеству объектов**.  **Очень затратна вычислительно, но дает максимально "честную" оценку** при очень малых данных.

### 3.4. ⚖️ Дисбаланс классов: стратегии борьбы

*   **Дисбаланс классов (Class Imbalance):**  Ситуация, когда **один класс встречается в данных гораздо чаще, чем другой (другие)**.  **Распространенная проблема в задачах классификации** (например, обнаружение мошенничества, медицинская диагностика редких заболеваний, отток клиентов).  **Accuracy становится ненадежной метрикой** в условиях дисбаланса.  Модель, предсказывающая всегда мажоритарный класс, может иметь высокую Accuracy, но быть бесполезной на практике.  **Важнее использовать Precision, Recall, F1-score, AUC-ROC, AUC-PR.**

*   **Стратегии борьбы с дисбалансом классов:**
    *   **Oversampling (Перевыборка майноритарного класса):**  **Увеличение количества объектов майноритарного (меньшего) класса** искусственным путем.
        *   **Random Oversampling:**  **Случайное копирование** объектов майноритарного класса.  **Простой метод, но может привести к переобучению**, так как модель "запоминает" скопированные объекты.
        *   **SMOTE (Synthetic Minority Over-sampling Technique):**  **Генерация синтетических объектов майноритарного класса** на основе существующих.  **Более продвинутый метод, чем Random Oversampling, помогает избежать переобучения.**
    *   **Undersampling (Недовыборка мажоритарного класса):**  **Уменьшение количества объектов мажоритарного (большего) класса** путем случайного удаления.  **Может привести к потере информации**, так как удаляются объекты мажоритарного класса.  **Подходит, когда данных очень много, и потеря части мажоритарного класса не критична.**
        *   **Random Undersampling:**  **Случайное удаление** объектов мажоритарного класса.
        *   **Tomek links:**  Удаление объектов мажоритарного класса, образующих "пары Томека" с объектами майноритарного класса (пары близких объектов разных классов).  **Более "умный" метод, чем Random Undersampling, помогает "очистить" границу классов.**
    *   **Class weights (Веса классов):**  **Присвоение разных весов классам** при обучении модели.  **Майноритарному классу дается больший вес, мажоритарному – меньший**.  **Многие ML-алгоритмы (например, Logistic Regression, SVM, Random Forest, XGBoost) имеют параметр `class_weight`**.  **Эффективный и простой способ борьбы с дисбалансом.**
    *   **Cost-sensitive learning (Обучение с учетом стоимости ошибок):**  **Назначение разных "стоимостей" (штрафов) для разных типов ошибок (False Positives, False Negatives)**.  **Более гибкий подход, чем class weights**, позволяет задавать разные штрафы для FP и FN.  **Редко поддерживается "из коробки" в ML-библиотеках, требует кастомной реализации.**

### 4.1. 🤕 Недообучение (Underfitting): "Слишком простая модель"

*   **Суть:** Модель **слишком проста**, чтобы уловить **сложные закономерности** в данных. Она **недостаточно "обучилась"**, чтобы хорошо работать даже на **обучающих данных**.
    *   **Bias (Смещение) при недообучении:**  **Высокий Bias**.  Модель делает **систематические ошибки** из-за **ограниченности** своей выразительной способности.  **Аналогия:  Прямая линия пытается аппроксимировать квадратичную зависимость.**  Модель "смещена" в сторону **слишком простых решений**.
    *   **Variance (Дисперсия) при недообучении:** **Низкая Variance**.  Модель **стабильна**, ее качество **не сильно меняется** при изменении обучающей выборки, но стабильно **плохое**.  **Аналогия:  Прямая линия, построенная по разным подвыборкам данных, будет незначительно отличаться.**
    *   **Решение:** **Усложнить модель:** Выбрать более **сложный алгоритм**, добавить **больше признаков**, **уменьшить регуляризацию** (если она есть).

### 4.2. 🤪 Переобучение (Overfitting): "Слишком сложная модель"

*   **Суть:** Модель **слишком сложна** и **"запоминает"** обучающие данные, включая **шум и случайные флуктуации**. Она отлично работает на **обучающей выборке**, но **плохо обобщает** на **новые данные**.
    *   **Bias (Смещение) при переобучении:** **Низкий Bias**.  Модель **хорошо приближает** обучающие данные, делает **мало систематических ошибок** на train.  **Аналогия:  Кривая высокого порядка идеально "подгоняется" под все точки обучающей выборки.**
    *   **Variance (Дисперсия) при переобучении:** **Высокая Variance**.  Модель **очень чувствительна** к изменениям в обучающей выборке.  Небольшие изменения в train могут **сильно изменить** качество модели на test.  Модель "нестабильна".  **Аналогия:  Кривая высокого порядка, построенная по разным подвыборкам, будет сильно меняться, "извиваться" по-разному.**
    *   **Решение:** **Упростить модель и/или увеличить данные:** **Упростить модель** (например, уменьшить степень полинома, глубину дерева, количество нейронов), **Регуляризация** (L1, L2), **Больше данных**,  **Feature selection/reduction**, **Dropout** (для нейросетей), **Early stopping** (остановка обучения до переобучения).

### 4.3. ⚖️ Bias-Variance Tradeoff:  Поиск баланса

*   **Bias и Variance – две составляющие ошибки модели.**  Общая ошибка модели складывается из **смещения (Bias)**, **дисперсии (Variance)** и **неустранимой ошибки ( irreducible error)**, связанной с шумом в данных.
    *   **Bias-Variance Tradeoff** – это **компромисс** между смещением и дисперсией.  **Увеличение сложности модели** обычно **снижает Bias**, но **увеличивает Variance**.  **Уменьшение сложности модели** обычно **увеличивает Bias**, но **снижает Variance**.
    *   **Цель:**  Найти **оптимальную сложность модели**, которая **минимизирует общую ошибку**, находя баланс между Bias и Variance.  Это соответствует поиску "золотой середины" между недообучением и переобучением.  **"Хорошая" модель – это модель с низким Bias и низкой Variance.**  **На практике часто приходится идти на компромисс.**

### 4.4. 📈 Learning Curves (Кривые обучения): Инструмент диагностики Bias и Variance

    *   **Кривые обучения** показывают, как **качество модели** (например, ошибка) **меняется в зависимости от размера обучающей выборки**.  Обычно строят две кривые:
        *   **Кривая обучения (Training Curve):**  Качество модели, измеренное на **обучающей выборке**, в зависимости от размера train.
        *   **Кривая валидации (Validation Curve):**  Качество модели, измеренное на **валидационной выборке**, в зависимости от размера train.
    *   **Диагностика по Learning Curves:**
        *   **Недообучение (High Bias):**
            *   Обе кривые (train и validation) **сходятся на высоком уровне ошибки** и **близки друг к другу**.
            *   **Разрыв между кривыми небольшой**.
            *   **Кривые "выходят на плато"** на **неудовлетворительном уровне**.
            *   **Увеличение размера данных не сильно помогает** улучшить качество.  **Нужно усложнять модель.**
        *   **Переобучение (High Variance):**
            *   **Кривая обучения** достигает **очень низкого уровня ошибки**, часто **близко к нулю**.
            *   **Кривая валидации** останавливается на **заметно более высоком уровне ошибки** или даже начинает **расти** после определенного момента.
            *   **Большой разрыв между кривыми**.  **Разрыв увеличивается с ростом размера обучающей выборки.**
            *   **Увеличение размера данных может помочь** уменьшить разрыв и улучшить обобщение, **но не всегда достаточно**.  **Нужно упрощать модель и/или применять регуляризацию.**
        *   **"Хорошее" обучение (Low Bias & Low Variance):**
            *   Обе кривые **сходятся на низком уровне ошибки** и **близки друг к другу**.
            *   **Разрыв между кривыми небольшой**.
            *   **Кривые "выходят на плато"** на **низком, удовлетворительном уровне**.
            *   **Дальнейшее увеличение размера данных может незначительно улучшить качество.**

## 5. 📝 Сводка изученных метрик и перспективы

В данном разделе были изучены следующие группы метрик для оценки качества моделей:

*   **Метрики Регрессии:**
    *   MSE (Mean Squared Error)
    *   RMSE (Root Mean Squared Error)
    *   MAE (Mean Absolute Error)
    *   MAPE (Mean Absolute Percentage Error)
    *   R-squared (Коэффициент детерминации)
    *   Adjusted R-squared (Скорректированный R-squared)

*   **Метрики Классификации:**
    *   Accuracy
    *   Confusion Matrix (матрица ошибок)
    *   Precision
    *   Recall
    *   F1-score
    *   ROC AUC
    *   PR AUC
    *   Log Loss (Cross-Entropy Loss)
    *   Мультиклассовые метрики (Macro, Micro, Weighted Average)