# 📊 Визуализация данных: Matplotlib и Seaborn для анализа данных

**Визуализация данных** играет **критически важную роль** в машинном обучении (ML). Это не просто красивые картинки, а **инструмент** для понимания данных, диагностики моделей и коммуникации результатов.

## 1. 🌟 Важность визуализации данных в ML

Визуализация — **неотъемлемая часть** ML-проектов, которая помогает:
* **Понимать данные (EDA):** Исследовать распределения, зависимости, выбросы, качество данных.
* **Коммуницировать:** Наглядно представлять выводы для разных аудиторий.
* **Диагностировать модели:** Оценивать метрики (ROC, PR-кривые), ошибки, важность признаков.
* **Генерировать идеи:** Видеть новые гипотезы и подходы.

**Пример:** Гистограмма может показать скошенность признака, а тепловая карта — избыточную корреляцию, что влияет на выбор модели и предобработку.

---

## 2. 📚 Теоретическая база визуализации

### 2.1. 🎓 Основы статистики для графиков
Визуализация тесно связана со статистикой:
- **Распределения:** 
  - *Нормальное* (колоколообразное, симметричное).
  - *Скошенное* (асимметричное, длинный "хвост" справа или слева).
  - *Бимодальное* (два пика).
  - *Равномерное* (все значения равновероятны).
- **Меры центральной тенденции:** Среднее (mean), медиана (median), мода (mode). Графики помогают увидеть их различия.
- **Дисперсия и выбросы:** Стандартное отклонение (std), квартили (Q1, Q3), межквартильный размах (IQR). Boxplot’ы показывают эти характеристики.
- **Корреляция:** Линейная связь между переменными (от -1 до 1). Тепловые карты визуализируют матрицу корреляций.

### 2.2. 🧠 Почему визуализация работает?
- Люди лучше воспринимают **визуальные паттерны**, чем таблицы чисел (теория гештальт-психологии).
- Графики упрощают анализ сложных данных, выявляя **тренды**, **аномалии** и **структуры**.

**Вопрос:** Какую форму распределения ты ожидаешь для возраста людей? Почему?

---

## 3. 📈 Matplotlib - Основа визуализации в Python

**Matplotlib** — фундаментальная библиотека для графиков в Python. Она дает **полный контроль** над визуализацией.

* **Модуль:** `import matplotlib.pyplot as plt`.
* **Основные типы графиков:**
  - **Линейные (`plt.plot`):** Тренды, временные ряды.
  - **Точечные (`plt.scatter`):** Зависимости, кластеры, выбросы.
  - **Гистограммы (`plt.hist`):** Распределения (аналогия: делим данные на "корзины", как людей по росту).
  - **Коробчатые (`plt.boxplot`):** Квартили, выбросы.
  - **Столбчатые (`plt.bar`):** Категории, частоты.
  - **Контурные (`plt.contour`):** Плотность или зависимости в 2D (например, функции потерь).
  - **3D-графики (`Axes3D`):** Визуализация трехмерных данных.

#### ✍️ Пример с Matplotlib:
```python
import matplotlib.pyplot as plt
import numpy as np

# Линейный график и гистограмма
x = np.linspace(0, 10, 100)
y = np.sin(x)
plt.plot(x, y, label='sin(x)', color='blue')
plt.hist(y, bins=20, alpha=0.5, label='Распределение')
plt.title('Синусоида и её распределение')
plt.xlabel('X')
plt.ylabel('Y')
plt.legend()
plt.show()
```

**Настройка:** `plt.title()`, `plt.xlabel()`, `color`, `linestyle`, `plt.grid()`.

**Когда использовать:** Для детальной настройки или нестандартных графиков.

**Задание:** Построй линейный график для `cos(x)` и сравни с `sin(x)`.

---

## 4. ✨ Seaborn - Статистическая визуализация

**Seaborn** — высокоуровневая библиотека на основе Matplotlib, упрощает создание статистических графиков.  
* Импорт: `import seaborn as sns`.  
* Работает с Pandas DataFrame.  
* Графики эстетичны "из коробки".

#### Основные графики:
- **Тепловые карты (`sns.heatmap`):** Корреляции.
- **Парные графики (`sns.pairplot`):** Попарные зависимости.
- **Гистограммы с KDE (`sns.histplot`):** Распределения с плотностью.
- **Коробчатые (`sns.boxplot`):** Сравнение групп, выбросы.
- **Violinplot (`sns.violinplot`):** Распределения + форма.
- **Линейные (`sns.lineplot`):** Тренды с доверительными интервалами.
- **Swarmplot (`sns.swarmplot`):** Точки без наложения для категорий.

#### ✍️ Пример с Seaborn для ML:
```
import seaborn as sns
import pandas as pd
import matplotlib.pyplot as plt

# Данные
data = pd.DataFrame({
    'feature1': [1, 2, 3, 4, 5, 10],
    'feature2': [2, 4, 6, 8, 10, 20],
    'class': ['A', 'A', 'A', 'B', 'B', 'B']
})

# Парный график
sns.pairplot(data, hue='class')
plt.show()
```
**Когда использовать:** Для быстрого EDA, статистических графиков, работы с DataFrame.

**Вопрос:** Чем `violinplot` отличается от `boxplot`? Когда выбрать каждый?

---

## 5. 📌 Ключевые типы графиков и их применение в ML

| Тип графика            | Matplotlib         | Seaborn            | Применение в ML                          |
|-----------------------|--------------------|--------------------|------------------------------------------|
| Линейный              | `plt.plot()`       | `sns.lineplot()`   | Тренды, временные ряды                  |
| Точечный              | `plt.scatter()`    | `sns.scatterplot()`| Зависимости, кластеры                   |
| Гистограмма           | `plt.hist()`       | `sns.histplot()`   | Распределение признака                  |
| Коробчатый            | `plt.boxplot()`    | `sns.boxplot()`    | Выбросы, сравнение групп                |
| Столбчатый            | `plt.bar()`        | `sns.barplot()`    | Сравнение категорий                     |
| Тепловая карта        | -                  | `sns.heatmap()`    | Корреляции между признаками             |
| Парный график         | -                  | `sns.pairplot()`   | Попарные зависимости (EDA)              |
| KDE-график            | -                  | `sns.kdeplot()`    | Плотность распределения                 |
| Violinplot            | -                  | `sns.violinplot()` | Форма распределения по группам          |
| Контурный             | `plt.contour()`    | -                  | Плотность, функции потерь               |
| 3D-график             | `Axes3D.plot()`    | -                  | Трехмерные зависимости                  |

#### ✍️ Пример 3D-графика:
```
from mpl_toolkits.mplot3d import Axes3D
import matplotlib.pyplot as plt
import numpy as np

fig = plt.figure()
ax = fig.add_subplot(111, projection='3d')
x = np.linspace(-5, 5, 100)
y = np.linspace(-5, 5, 100)
X, Y = np.meshgrid(x, y)
Z = np.sin(X) * np.cos(Y)
ax.plot_surface(X, Y, Z, cmap='viridis')
plt.show()
```

---

## 6. 🔑 Главный навык - Интерпретация графиков

**Создание графиков — полдела.** Важно уметь их читать:
- **Распределения:** Нормальное, скошенное, бимодальное? Как это влияет на модель?
- **Зависимости:** Линейные, нелинейные? Есть ли корреляция?
- **Выбросы:** Ошибки или значимые точки? Нужно ли их удалять?
- **Тренды:** Растут, падают, циклические?

**Вопросы для анализа:**
- Что показывает график?
- Какие закономерности видны?
- Как это помогает в ML?

#### ✍️ Пример интерпретации:
Если `heatmap` показывает корреляцию 0.9 между двумя признаками, это сигнал к удалению одного из них (мультиколлинеарность). Если `boxplot` выявляет выбросы, их можно исследовать перед обучением.

---

## 7. 🧪 Практика в ML-контексте

#### ✍️ Анализ корреляций:
```
import seaborn as sns
import pandas as pd
import matplotlib.pyplot as plt

# Данные
data = pd.DataFrame({
    'f1': [1, 2, 3, 4, 5],
    'f2': [2, 4, 6, 8, 10],
    'f3': [1, 3, 2, 4, 5]
})
corr = data.corr()

# Тепловая карта
sns.heatmap(corr, annot=True, cmap='coolwarm')
plt.title('Матрица корреляций')
plt.show()
```

**Вывод:** Высокая корреляция между `f1` и `f2` (0.99) — возможна мультиколлинеарность.

---